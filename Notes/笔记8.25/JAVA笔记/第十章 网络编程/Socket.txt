 两个java应用程序可以通过一个双向的网络通信实现数据交换 双向链成为SOCKET
 Socket通常来实现client-server连接
 只有tcp分 client server

java.net包中定义两个类 Socket 和 ServerSocket 分别用来实现双向链接client server端
端口号 用来区分同一个机器上的程序  不要用1024以下的端口号

TCP UDP 两种端口
	TCP UDP 相互不影响 

先启动server 再 启动client 

Socket也需要关闭

阻塞式 死等待 不会继续进行 效率不高
	上一个连接没有完成 下一个连接传输不能进行（现在有异步
	用多线程处理 大多数网络使用异步io编程。
	  e