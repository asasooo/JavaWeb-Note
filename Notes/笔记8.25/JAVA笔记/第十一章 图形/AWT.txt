AWT 抽象窗口开发包

AWT没有实现完全跨平台

新开发包 javax.swing swing 离不开AWT

界面开发程序一般把两者都用


component 可以进行图像交互的对象


panel不能作为独立的窗口显示

dialog 对话框
最基本的组成部分 Component  Container 是 Component的子类
	两种主要的Comtainers window  panel

Frame  是window的子类 Frame 是可以独立显示的窗口

	Frame 方法：
		set 

