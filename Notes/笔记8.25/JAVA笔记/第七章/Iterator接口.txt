实现了collection的 都有 iterator方法

iterator是一个接口 都具有 
	hashNext
	next
	remove

arraylist使用数组作为底层空间
linkedlist使用脸变作为底层空间

遍历 按顺序查找

iterator实现遍历查找 作为遍历元素的方法 迭代法 可以看作一个指针
执行时 会进行锁定 不能调用上一级的名称

用于遍历容器中的每个对象而不用考虑容器中对象的个数，而且保护数据不显示的表现出来。