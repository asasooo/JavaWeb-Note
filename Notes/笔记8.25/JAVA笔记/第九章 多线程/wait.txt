 控制线程 wait 方法

	来自 Object  任何一个对象都可以调用wait方法

	当前正在访问 this。wait  当前的线程停止住
	只有锁定住的 线程 才可以wait
		wait 时 锁不再归这个 方法所有
		sleep 时 锁归这个 方法所有

	wait 之后 用 notify 方法 	
	  notify 的 含义 ：叫醒wait的线程 让他继续工作
		notify也是Object的 方法


wait时 可以访问锁定对象
Sleep  不可以访问锁定对象

synchronized